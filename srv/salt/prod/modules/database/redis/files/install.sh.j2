#!/bin/bash

cd /usr/src/redis-6.2.6
make MALLOC=libc USE_SYSTEMD=yes && \
make install PREFIX={{ pillar['redis_install_dir'] }}
echo "export PATH={{ pillar['redis_install_dir'] }}/bin:\$PATH" > /etc/profile.d/redis.sh
